{
  "name": "flights",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "repository": { "type": "git", "url": "git+https://github.com/proappstore-online/flights.git" },
  "scripts": {
    "dev": "pnpm --filter @flights/web dev",
    "prebuild": "npx -y @proappstore/cli@latest check",
    "build": "pnpm --filter @flights/web build",
    "preview": "pnpm --filter @flights/web preview",
    "typecheck": "pnpm --filter @flights/web exec tsc -b",
    "test": "pnpm --filter @flights/web exec tsc -b"
  }
}
